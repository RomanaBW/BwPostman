---
- hosts: localhost
  connection: local
  vars_files:
#    - ../docker/vars/vars_versions.yml
#    - ../docker/vars/vars_current.yml
    - vars/tester-image-vars.yml
    - vars/tester-test-vars.yml

  tasks:
#    - name: Get variables if joomla main version is provided as extra_vars
#      include_vars: ../docker/vars/vars_previous.yml
#      when: joomla_version is defined and joomla_version[0] == '5'

    - name: remove all test containers (db, webserver, tester) and network
      include_role:
        name: romanabw.remove-complete-infrastructure
      vars:
        source: "{{ smoke_source }}"
        target: "{{ smoke_target }}"
