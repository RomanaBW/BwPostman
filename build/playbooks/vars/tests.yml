---
log_path: '/data/logs'
acceptance_manifest_path: "/repositories/BwPostman/tests"
codeception_manifest_path: "/repositories/BwPostman"
record_path: "/data/repositories/artifacts/bwpostman/j{{ joomla_version }}_bwpm{{ version_number }}/videos"
test_log_path: "/repositories/artifacts/bwpostman/j{{ joomla_version }}_bwpm{{ version_number }}/logs"

codecept_path_tests: "tests"
codecept_path_data: "../artifacts/bwpostman"
codecept_path_support: "tests/_support"
codecept_path_output: "./repositories/artifacts/bwpostman/j{{ joomla_version }}_bwpm{{ version_number }}/{{ test_suite }}/logs"

test_db_host: "localhost"
test_db_dbname: "joomlatest"
test_db_user: "root"
test_db_password: "{{ vault_test_db_password }}"

screen_size: '1440x900'
display: ':45'

# runtime install ? min
# runtime update ? min
# runtime uninstall ? min
# runtime options ? min
# runtime lists 11 min
# runtime maintenance 3 min
# runtime frontend 2 min
# runtime details 16 min
# runtime user2subscriber 10 min
# runtime access ?? min

test_file:
  - suite: install
    testsuite_file: Backend/TestInstallationCest
  - suite: update
    testsuite_file: Backend/TestUpdateCest
  - suite: uninstall
    testsuite_file: Backend/TestDeinstallationCest
  - suite: options
    testsuite_file: Backend/TestOptionsCest
  - suite: lists
    testsuite_file: Backend/Lists
  - suite: maintenance
    testsuite_file: Backend/TestMaintenanceCest
  - suite: restore
    testsuite_file: Backend/TestMaintenanceCest::restoreTables
  - suite: frontend
    testsuite_file: Frontend
  - suite: details
    testsuite_file: Backend/Details
  - suite: user2subscriber
    testsuite_file: User2Subscriber
  - suite: access
    testsuite_file: Backend/Access
#  - personalize:
#  - restore:
#  - module_subs:
#  - module_overview:

test_suites:
  - name: smoke
    tests:
      - install
      - restore
      - uninstall

  - name: accept1
    tests:
      - install
      - restore
      - lists
      - maintenance

  - name: accept2
    tests:
      - install
      - restore
      - details

  - name: accept3
    tests:
      - install
      - restore
      - frontend
      - user2subscriber
  #  - personalize

  - name: accept4
    tests:
      - install
      - restore
      - access

