---
- name: Set archive source
  set_fact:
    archive_source: "https://www.joomlacontenteditor.net/downloads/editor/core?task=release.download&id=259"

- name: get JCE installation package
  get_url:
    url: "{{ archive_source }}"
    dest: "{{ codecept_path_data }}/pkg_jce.zip"
    force: yes

