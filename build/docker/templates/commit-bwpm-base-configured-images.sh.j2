#!/bin/bash
#
# This is part of step 3 to nail down the created images with installed and adjusted Joomla
#
# Tag is php version and Joomla version (with dots), separated by a hyphen
#

docker commit -a 'Romana Boldt info@boldt-webservice.de' {{ bwpm_joomla_name }} universe3:5000/romana/bwpm-base-files:{{ bwpm_php_version }}-{{ bwpm_joomla_version }}
docker commit -a 'Romana Boldt info@boldt-webservice.de' {{ bwpm_db_name }} universe3:5000/romana/bwpm-base-tables:{{ bwpm_php_version }}-{{ bwpm_joomla_version }}
