---
class_name: AcceptanceTester

paths:
  log: output
  data: ../repositories/artifacts/bwpostman4/data

modules:
  enabled:
    - \Helper\Acceptance
    - WebDriver
    - Db
    - Asserts
    - Filesystem

  # Default configuration is for simple joomla container tests (BwPostman component, modules, 'normal' plugins)
  config:
    Db:
      dsn: 'mysql:host=172.118.5.10;dbname=joomlatest'
      user: 'tester'
      password: 'barbamama'

      populate: false
      cleanup: false
      reconnect: true

    WebDriver:
      url: http://172.118.5.11

      browser: chrome
      port: 4445
      capabilities:
        "goog:chromeOptions":
          args: ["--window-size={{ screen_size_opts }}", "--no-sandbox"]
          prefs:
            download.default_directory: "/data/output"

      connection_timeout: 60
      restart: false
