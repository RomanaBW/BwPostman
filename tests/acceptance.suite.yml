---
# Codeception Test Suite Configuration
#
# Suite for acceptance tests.
# Perform tests in browser using the WebDriver or PhpBrowser.
# If you need both WebDriver and PHPBrowser tests - create a separate suite.

class_name: AcceptanceTester

paths:
  log: tests/_output/j400rc-2_bwpm400
  data: tests/_data

modules:
  enabled:
    - \Helper\Acceptance
    - WebDriver
    - Db
    - Asserts
    - Filesystem
  #      - MailCatcher
  #      - \Helper\MailCatcher

  # Default configuration is for simple joomla container tests (BwPostman component, modules, 'normal' plugins)
  config:
    Db:
      dsn: 'mysql:host=mysqldbbwpm4;dbname=joomlatest'
      user: tester
      password: barbamama

      populate: false
      cleanup: false
      reconnect: true

    WebDriver:
      url: 'http://172.18.0.28'

      browser: chrome
      port: 4445
      capabilities:
        chromeOptions:
          args: ["--window-size=1920,1080"]
          prefs:
            download.default_directory: "/tests/tests/_output"


      connection_timeout: 60
      restart: false

#        browser: phantomjs
#        port: 4447
#        browser: firefox
#        port: 4444

#      MailCatcher:
#        url: 'http://172.18.0.28'
#        port: '1080'

